import { AccessibleEventTriggerDirective } from './accessible-event-trigger.directive';

describe('AccessibleEventTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessibleEventTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
